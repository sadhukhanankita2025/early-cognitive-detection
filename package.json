{
    "name": "early-cognitive-decline-detection",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "npm --prefix frontend run dev -- --port 5175 --host 0.0.0.0",
        "build": "npm --prefix frontend run build",
        "preview": "npm --prefix frontend run preview",
        "lint": "npm --prefix frontend run lint"
    }
}